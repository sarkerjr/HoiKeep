// project import
import student from './student';
import dashboard from './dashboard';
import fee from './fee';
import admin from './admin';
import other from './other';

const menuItems = {
  items: [dashboard, student, fee, admin, other],
};

export default menuItems;
